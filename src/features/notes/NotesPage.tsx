import { EmptyState } from '../../components/ui/EmptyState'

export function NotesPage() {
  return (
    <EmptyState
      title="No notes yet"
      hint="Quick capture and note→task conversion arrive in milestone 3."
    />
  )
}
